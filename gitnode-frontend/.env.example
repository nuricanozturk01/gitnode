# Copy to .env (optional — defaults work for local dev)

# API base URL (no trailing slash)
VERCEL_API_BASE_URL=http://localhost:8080

# SSH remote hint shown in clone URLs
VERCEL_GIT_SSH_URL=git@localhost:2222


GITNODE_SSO_SAML_REGISTRATION_ID=saml
