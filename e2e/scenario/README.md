# Scenario E2E (browser flows)

Reserved for future Playwright tests that exercise the Angular UI end-to-end (login, repo browse, PR workflow, etc.).

API coverage lives under `../api/`. Run API tests with:

```bash
cd e2e && pnpm test:e2e:api
```
