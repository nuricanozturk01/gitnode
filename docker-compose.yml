services:
  postgres:
    container_name: gitnode-postgres
    hostname: gitnode-postgres
    build:
      context: ./postgres
      dockerfile: Dockerfile
    environment:
      POSTGRES_DB: gitnode
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin123
    command: >
      postgres
      -c shared_preload_libraries=pgaudit
      -c pgaudit.log=write,ddl,role
      -c log_destination=stderr
      -c logging_collector=on
      -c log_directory=/var/log/postgresql
      -c log_filename=postgresql-%Y-%m-%d.log
      -c log_rotation_age=1d
      -c log_truncate_on_rotation=on
      -c log_min_duration_statement=500
    volumes:
      - postgres-logs:/var/log/postgresql
    ports:
      - "5432:5432"
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U admin -d gitnode']
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - gitnode

  redis:
    container_name: gitnode-redis
    image: redis:7-alpine
    command: redis-server --save "" --maxmemory 512mb --maxmemory-policy volatile-lru
    ports:
      - "6379:6379"
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - gitnode

networks:
  gitnode:
    name: gitnode
    driver: bridge

volumes:
  postgres-logs:
    name: gitnode-postgres-logs
  gitnode-repos:
    name: gitnode-repos
  runner-work:
    name: gitnode-runner-work
